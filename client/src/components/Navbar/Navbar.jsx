import React from 'react';
import {
    ClerkLoaded,
    SignedIn,
    SignedOut,
    SignInButton,
    SignOutButton,
    SignUpButton,
    UserButton
} from "@clerk/clerk-react";

const Navbar = () => {
    return (
        <header>

        </header>
    );
};

export default Navbar;